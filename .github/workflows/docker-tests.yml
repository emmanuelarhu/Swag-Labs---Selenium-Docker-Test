name: Tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t swaglabs/selenium-tests:1.0 .
      - run: docker run --rm --name swaglabs-test-runner -v $PWD/target:/app/target swaglabs/selenium-tests:1.0