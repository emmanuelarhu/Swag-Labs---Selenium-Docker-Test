name: Tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t manuelarhu/saucedemo-selenium-tests .
      - run: docker run --rm --name saucedemo-ci-runner -v $PWD/target:/app/target manuelarhu/saucedemo-selenium-tests